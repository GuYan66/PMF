# 整理与验证记录

日期：2026-09-25。验证环境：Linux，Python 3.10.21，PyTorch 2.4.1+cu118；本次模型测试使用 CPU。

## 已完成

- 5 项 unittest 全部通过。覆盖 padding 排除与缺失零位置保留、训练集统计量、单样本与双头梯度、checkpoint 重载、CSV 表头/原始数值/缺失 Corr/多种子汇总。
- 合成数据完整走通两个种子的 CPU 训练、保存、重载、valid/test/test2 评测；两个种子分别保留权重与配置。
- CPU 路径测试禁止调用 GPU 探测和 CUDA set_device，实际通过；不是用空操作掩盖 CPU 错误。
- 独立训练脚本 dry-run 与独立预测脚本均通过测试；单样本预测与框架接口结果一致。
- 构建 MMSA 2.2.1+pmf.1 wheel 成功。wheel 安装在临时虚拟环境，从 site-packages 导入，验证 PMF 注册、JSON 配置及 162,436 参数。
- 将源码复制到独立临时目录，使用上述安装后的 wheel 再运行 5 项测试，全部通过。
- 新训练脚本对真实附件 2 和外部 test2 完成 dry-run：train 3395、valid 728、test 727、test2 30；极性输出 [64,3]，幅度输出 [64]。
- 共享文件扫描未发现本项目个人服务器路径；保留原始 MIT LICENSE 和上游出处。数据、参考标签、权重、环境文件和 .git 不包含在源码快照中。
- 其他旧 baseline 模型文件保持上游版本，PMF 为唯一新增的双头 baseline。

## 验证边界

没有执行真实竞赛数据上的完整训练或评估模型优劣。临时安装测试复用了服务器现有依赖，不等同于从零下载全部依赖的测试。
Windows CI 已配置，但本地没有宣称已完成 Windows 实机测试；需在团队仓库推送后查看 Actions 状态。
本次没有创建、提交或推送远程 Git 仓库，也没有发布 PyPI。
